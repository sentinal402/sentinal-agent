export const DEFAULT_CHAIN_ID = 1;
export const SAFE_THRESHOLD = 25;
export const WARNING_THRESHOLD = 60;
export const CRITICAL_THRESHOLD = 85;

export const DEFAULT_EMBEDDING_DIM = 128;
export const MAX_SIMULATED_STEPS = 64;

export const X402_HEADERS = {
  request: "x-402-request",
  verified: "x-402-verified"
};

export const AI_ENGINE_ENDPOINT = "/ai/classify";
